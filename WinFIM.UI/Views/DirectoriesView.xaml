<UserControl x:Class="WinFIM.UI.Views.DirectoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Gerenciamento de Diretórios" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Adicione os diretórios que deseja monitorar." Margin="0,0,0,10"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding NewDirectoryPath, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="Adicionar Diretório" Command="{Binding AddDirectoryCommand}" Padding="10,5"/>
            </Grid>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Directories}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Caminho" Binding="{Binding Path}" Width="*"/>
                <DataGridTextColumn Header="Ativo" Binding="{Binding IsActive}" Width="100"/>
                <DataGridTemplateColumn Header="Ações" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Pausar/Retomar" Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="0,0,5,0" Padding="5,2"/>
                                <Button Content="Remover" Command="{Binding DataContext.RemoveDirectoryCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Padding="5,2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
