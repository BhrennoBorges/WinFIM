<UserControl x:Class="WinFIM.UI.Views.BaselineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="Criação de Baseline" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Gere uma referência confiável dos arquivos para o monitoramento." Margin="0,0,0,20"/>
            
            <Button Content="Criar Nova Baseline" Command="{Binding CreateBaselineCommand}" IsEnabled="{Binding IsProcessing, Converter={StaticResource InverseBooleanConverter}}" Padding="15,10" Width="200" HorizontalAlignment="Left" Margin="0,0,0,20"/>
            
            <TextBlock Text="Status do Processamento:" FontWeight="Bold"/>
            <TextBlock Text="{Binding ProgressText}" Margin="0,10,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
