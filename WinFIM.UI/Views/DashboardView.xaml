<UserControl x:Class="WinFIM.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="Visão Geral do WinFIM Guard" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <Border Background="#F0F0F0" Padding="15" Margin="0,0,0,10" CornerRadius="5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Diretórios Ativos Monitorados:" FontWeight="Bold" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding TotalDirectories}"/>
                </StackPanel>
            </Border>
            
            <Border Background="#F0F0F0" Padding="15" Margin="0,0,0,10" CornerRadius="5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total de Eventos Detectados:" FontWeight="Bold" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding TotalEvents}"/>
                </StackPanel>
            </Border>
            
            <Border Background="#F0F0F0" Padding="15" Margin="0,0,0,10" CornerRadius="5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Status da Baseline:" FontWeight="Bold" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding LatestSnapshotStatus}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
