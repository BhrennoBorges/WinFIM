<UserControl x:Class="WinFIM.UI.Views.EventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Eventos de Integridade" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="Atualizar Lista" Command="{Binding LoadEventsCommand}" Padding="10,5" Margin="0,0,10,0"/>
                <Button Content="Forçar Revalidação" Command="{Binding RunRevalidationCommand}" IsEnabled="{Binding IsRevalidating, Converter={StaticResource InverseBooleanConverter}}" Padding="10,5" Margin="0,0,10,0"/>
                <Button Content="Exportar para CSV" Command="{Binding ExportCsvCommand}" Padding="10,5"/>
            </StackPanel>
            <TextBlock Text="{Binding RevalidationProgress}" Foreground="DarkGray" Visibility="{Binding IsRevalidating, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Events}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data/Hora" Binding="{Binding DetectedAt, StringFormat=\{0:dd/MM/yyyy HH:mm:ss\}}" Width="150"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding EventType}" Width="100"/>
                <DataGridTextColumn Header="Severidade" Binding="{Binding Severity}" Width="100"/>
                <DataGridTextColumn Header="Caminho" Binding="{Binding FullPath}" Width="*"/>
                <DataGridTextColumn Header="Origem" Binding="{Binding DetectionSource}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
